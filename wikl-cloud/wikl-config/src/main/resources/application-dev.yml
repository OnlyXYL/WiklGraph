server:
  port: 8000

spring:
  application:
    name: wikl-config

#  security:
#    user:
#      name: admin
#      password: admin

  cloud:
    config:
      server:
        git:
          uri: https://www.github.com/OnlyXYL/config/
          username: only_xyl@163.com
          passphrase: xia18811599806
          #默认分支
#          default-label: master
          #配置文件所在根目录
          search-paths: config-repo/wikl-config/

eureka:
  instance:
    prefer-ip-address: true
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/