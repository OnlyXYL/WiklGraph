server:
  port: 8765
  servlet:
    context-path: /neo4j-gremlin

spring:
  application:
    name: neo4j-gremlin

eureka:
  client:
    service-url: http://localhost:8761/eureka/
  # 这只为prefer ip的话可以直接展示ip地址
  instance:
    prefer-ip-address: true