server:
  port: 8763
  servlet:
    context-path: /orientdb-gremlin

spring:
  application:
    name: orientdb-gremlin

eureka:
  client:
    service-url: http://localhost:8761/eureka/
  # 这只为prefer ip的话可以直接展示ip地址
  instance:
    prefer-ip-address: true